---
  name: ci

  on:
    pull_request:
    push:
      branches:
        - master

  jobs:
    delivery:
      runs-on: ubuntu-latest
      steps:
        - name: Check out code
          uses: actions/checkout@master
        - name: Run Chef Delivery
          uses: actionshub/chef-delivery@master
          env:
            CHEF_LICENSE: accept-no-persist

    # dokken:
    #   needs: [delivery]
    #   runs-on: ubuntu-latest
    #   strategy:
    #     matrix:
    #       os:
    #         - 'amazonlinux'
    #         - 'amazonlinux-2'
    #         # - 'debian-8'
    #         - 'debian-9'
    #         - 'debian-10'
    #         - 'centos-6'
    #         - 'centos-7'
    #         - 'centos-8'
    #         - 'oraclelinux-6'
    #         - 'oraclelinux-7'
    #         # - 'oraclelinux-8'
    #         - 'fedora-latest'
    #         - 'ubuntu-1604'
    #         - 'ubuntu-1804'
    #         - 'opensuse-leap-15'

    #       suite:
    #         - 'package'
    #         - 'service'
    #         - 'service-auth'
    #         - 'sup'
    #         - 'config'
    #         - 'config-auth'
    #         - 'user-toml'
    #         - 'license'
    #         - 'config-chef-13'
    #         - 'config-chef-13-auth'
    #     fail-fast: false

    #   steps:
    #     - name: Check out code
    #       uses: actions/checkout@master
    #     - name: Install Chef
    #       uses: actionshub/chef-install@master
    #     - name: Dokken
    #       uses: actionshub/kitchen-dokken@master
    #       env:
    #         CHEF_LICENSE: accept-no-persist
    #         KITCHEN_LOCAL_YAML: kitchen.dokken.yml
    #       with:
    #         suite: ${{ matrix.suite }}
    #         os: ${{ matrix.os }}


    windows:
      needs: [delivery]
      runs-on: windows-2019
      strategy:
        matrix:
          os:
            - 'windows-2019'
          suite:
            - 'win_config_auth'
            - 'win_config'
            - 'win_license'
            - 'win_package'
            - 'win_service'
            - 'win_sup'
            - 'win_user_toml'
        fail-fast: false
      steps:
        - name: Check out code
          uses: actions/checkout@master
        - name: Install Chef Workstation
          run: . { iwr -useb https://omnitruck.chef.io/install.ps1 } | iex; install -channel stable -project chef-workstation
          shell: powershell
        - name: Add Chef Workstation to PATH
          run: echo "##[add-path]C:\opscode\chef-workstation\bin\;"
          shell: powershell
        - name: CHEF_LICENSE
          run: chef env --chef-license accept
          shell: powershell
        - name: Chef Infra Client Run
          run: chef-solo -c test/solo.rb -o "test::${{ matrix.suite }}"
          shell: powershell
        - name: Integration Testing
          run: inspec exec test\integration\${{ matrix.suite }}\default_spec.rb
          shell: powershell


    # windows:
    #   needs: [delivery]
    #   runs-on: windows-2019
    #   strategy:
    #     matrix:
    #       os:
    #         - 'windows-2019'
    #       suite:
    #         - 'win-sup'
    #     fail-fast: false
    #   steps:
    #     - name: Check out code
    #       uses: actions/checkout@master
    #     - run: . { iwr -useb https://omnitruck.chef.io/install.ps1 } | iex; install -channel stable -project chef-workstation
    #       shell: powershell
    #     - name: Chef_Env_Path
    #       run: echo "##[add-path]C:\opscode\chef-workstation\bin\;"
    #       shell: powershell
    #     - name: CHEF_LICENSE
    #       run: chef env --chef-license accept
    #       shell: powershell
    #     - name: Docker_Opts
    #       run: $env:DOCKER_HOST="tcp://localhost:2375"
    #       shell: powershell
    #     - name: Dokken
    #       uses: actionshub/kitchen-dokken@master
    #       env:
    #         CHEF_LICENSE: accept-no-persist
    #         KITCHEN_LOCAL_YAML: kitchen.windows.yml
    #       with:
    #         suite: ${{ matrix.suite }}
    #         os: ${{ matrix.os }}
        # - name: Kitchen_File1
        #   run: mv kitchen.yml kitchen.old.yml
        #   shell: powershell
        # - name: Kitchen_File1
        #   run: mv kitchen.windows.yml kitchen.yml
        #   shell: powershell
        # - name: Kitchen_Docker_Gem
        #   run: chef gem install kitchen-docker
        #   shell: powershell
        # - name: Run kitchen list
        #   run: kitchen list
        #   shell: powershell
        # - name: kitchen_test
        #   run: kitchen test ${{ matrix.suite }}-${{ matrix.os }}
        #   shell: powershell
